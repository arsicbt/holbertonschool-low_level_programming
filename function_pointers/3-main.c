#include "calc.h"

/**
*
**/
int main(int argc, char *argv[])
{
	int a, b, result;
	int (*operation)(int)(int);

	if (argc > 4 || argc < 4)
	{
		printf("Error");
		exit(98);
	}

	a = atoi(argv[1]);
	b = atoi(argv[3]);


	if (check si loperateur est corect ..................
	{
		printf("Error");
		evit(99);
	}

	if (verifier la division/ modulo par 0 ..............
	{
		printf("Error");
		exit(100);
	}

	result = printf("%d\n", operation(a, b))

	return result;
}
