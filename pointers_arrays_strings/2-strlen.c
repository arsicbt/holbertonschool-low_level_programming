#include "main.h"
#include <stdio.h>

/**
 * _strlen - check the len of an str.
 * @s: char *
 * Return: the len.
 */
int _strlen(char *s)
{
	int i;
	int len;
	char str = *s;

	for (i = 0; i < str; i++)
	{
		len = i;
	}
	return (len);
}
