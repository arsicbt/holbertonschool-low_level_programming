#include "main.h"

/**
 *
 *
 *
**/
char *string_toupper(char *)
{
	char str;
	char * = str;
	int element;

	for (element = 0; str[element] != '\0'; element++)
	{
		if (c >= 65 && c <= 91)
	{
		_putchar(c);
		_putchar(':');
		_putchar(' ');
		_putchar('1');
		_putchar('\n');
		return (1);
	}
	else
	{
		_putchar(c);
		_putchar(':');
		_putchar(' ');
		_putchar('0');
		_putchar('\n');
		return (0);

	}
