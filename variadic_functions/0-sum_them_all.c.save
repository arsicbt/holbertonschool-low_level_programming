#include "main.h"
#include <stddef.h>
#include <stdio.h>

/**
**/
int sum_them_all(const unsigned int n, ...)
{
	unsigned int i;
	int sum;
	va_list list;

	arg_list = n;

	if (n == 0)
	{
		return (0);
	}

	for (i = 0; i < n; i++)
	{
		
	}
}
