#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
**/
char *create_array(unsigned int size, char c)
{
	int *p = malloc(size * sizeof(c));

	if (p == 0)
	{
		_putchar("Null");
	}
	return (p);
}
