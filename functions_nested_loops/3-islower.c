#include "main.h"
/**
 * _islower - check if is lower.
 * @c : index
 * Return: 1 or 0.
 */
int _islower(char c)
{
	if (c >= 'a' && c >= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
